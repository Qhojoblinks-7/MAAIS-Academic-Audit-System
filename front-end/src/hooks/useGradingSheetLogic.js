/**
 * useGradingSheetLogic (standalone hook)
 *
 * Re-exported from `src/pages/shared/useGradingSheetLogic`.
 * Kept in `src/hooks/` so external consumers (tests, feature modules)
 * can hook into grading-sheet logic without coupling to the page layer.
 *
 * Usage:
 *   import { useGradingSheetLogic } from '../../hooks/useGradingSheetLogic';
 */

export { useGradingSheetLogic } from '../pages/shared/useGradingSheetLogic';
